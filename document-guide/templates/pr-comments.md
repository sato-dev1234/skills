# PR #128 — 投稿するレビューコメント

[注文キャンセルの受付期限を追加](https://github.example/shop/orders/pull/128) · 対象差分 feature/cancel-window · 取得日 2026年8月20日

<!-- 書き手へ: 見出しは、投稿先のファイルパスと行番号である。見出しがファイルパスと行番号で1つに決まるためである。書き手が本文で番号による参照を使わないためである。

| 場面 | 書き手がすること |
|---|---|
| 常に | 通し番号を付けない |
| 指摘を投稿しないと決めた | その指摘を削らずに残す |
| 常に | コメントを、PRの差分に現れるファイルの順に並べ、同じファイルでは行の順に並べる |

| 投稿状態 | 見出しの末尾 |
|---|---|
| 指摘として成立するが投稿を見送った | 「（投稿しない）」を付ける |
| 投稿済み | 「（投稿済み）」を付ける |
| 未投稿 | 何も付けない |

書き手が後から見送った理由を辿るためである。 -->

<!-- 書き手へ: 書き手は、コメント本文を4バックティックのコードブロックで囲む。コードブロックが、中にsuggestionのブロックを含めても壊れず、そのまま投稿欄へ貼れるためである。

| 場面 | 書き手がすること |
|---|---|
| 常に | 各コメントをそれだけで意味が通るように書く |
| 常に | 他のコメントへの参照、この文書の外にある前提、指示語、「直上の」のような位置の思い込みを使わず、対象を具体的な名前と行番号で書く |
| 常に | 事実の報告で終わらせず、何をしてほしいかを書く |
| 常に | 結論を先に書き、前置きの後に本題を置かない |
| 修正案がある | 文章で指示せず、suggestionで示す | -->

<!-- 書き手へ: | 場面 | 書き手がすること |
|---|---|
| 対象リポジトリにレビューコメントの規約がある | ラベルをその規約に従わせる |
| 規約がない | ラベルを付けず、対応の要否と強さを「修正が必要です」「揃えてほしいです」「任意です」のように文末で表す | -->

<!-- 書き手へ: 対象行は、追加行が基本で、コンテキスト行も範囲に入る。

| 場面 | 書き手がすること |
|---|---|
| 常に | suggestionの対象行を、差分に表示される行にする |
| 常に | インデントを元コードと一致させる |
| 行を削る | 空のsuggestionを使わず、範囲を広げて残す行だけを書く |
| 常に | 引用を原文のままにし、読みやすさのために改行位置を変えない |
| 保存する前 | 次の4つを確かめる |

| 確かめること |
|---|
| 対象行が差分に含まれる |
| インデントが元コードと一致する |
| 削る例では、残す行がすべて元ファイルにある |
| 他のコメントへの参照が残っていない | -->

## client/src/features/orders/OrderList.tsx 43 行目（投稿済み）

````markdown
期限ちょうどの注文が期間内になります。要件は「期限以降は受付不可」で、詳細画面が呼ぶ `withinCancelWindow` は `>` で比較しているため、同じ注文で一覧と詳細画面の表示が食い違います。`>` に揃える修正が必要です。

```suggestion
    Date.parse(order.placedAt) + days * DAY_MS > nowMs;
```
````

## client/src/features/orders/OrderList.tsx 44 行目（投稿しない）

````markdown
期間内かを求めてから否定しているので、読む側は条件を2度読み替えます。期限切れを直接求める形にできます。動作は変わらないので任意です。

```suggestion
  const expired = days !== null &&
    Date.parse(order.placedAt) + days * DAY_MS <= nowMs;
```
````

## client/src/features/orders/cancelWindow.ts 3〜6 行目

````markdown
3〜5 行目のコメントは、関数の本体そのままです。落としてほしいです。

| コメント | 読める場所 |
|---|---|
| 日数が null なら期限なし | `if (days === null) return true;` |
| 注文日時に日数を足した時刻より前か | 比較式 |
| 判定時刻は呼び出し側から受け取る | 引数 `nowMs` |

```suggestion
export function withinCancelWindow(placedAt: string, days: number | null, nowMs: number) {
```
````

## client/src/features/orders/cancelWindow.test.ts 12 行目

<!-- 書き手へ: 確認状況は、本文で確定した形式を保つ。この見本では、各条件に「期待する結果」と「テストで通ったか」が対応するため、表で示している。

| 場面 | 書き手がすること |
|---|---|
| 常に | 表から読めること（通っていない件数、通っていない入力の一覧）を本文に書かない |
| 常に | 表が何を示すかの説明と依頼を本文に残す | -->

````markdown
以下が `withinCancelWindow` の境界条件のカバー状況です。期限と同時刻のケースを足してほしいです。

| 判定時刻 | 期待する結果 | テストで通ったか |
|---|---|---|
| 期限の直前 | 期間内 | ○ |
| 期限と同時刻 | 期間外 | × |
| 期限の直後 | 期間外 | ○ |
| 日数が未設定 | 期間内 | ○ |
````

## server/src/orders/OrderCancelService.ts 18〜19 行目

<!-- 書き手へ: | 場面 | 書き手がすること |
|---|---|
| 修正案が決まらない | そのコメントにsuggestionを置かず、確認してほしいことを文で書く |
| 常に | 確認したい理由を要件や設計の記述と対応させる | -->

````markdown
`tenant.settings.cancelWindowDays` がどの時点の値かを確認したいです。`TenantContext` がサインイン時に作られる値なら、設計資料の「サーバーは要求ごとに最新の設定を読む」を満たしません。その場合、サーバーは管理者が期間を変えた直後の要求を変更前の値で判定します。要求ごとに `tenant_settings` を読んでいることを教えてください。
````
